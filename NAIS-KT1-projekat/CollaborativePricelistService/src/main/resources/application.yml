server:
  port: 8090

spring:
  redis:
    host: redis
    port: 6379
    timeout: 2000ms
  data:
    redis:
      repositories:
        enabled: true

# Default cache TTL (used by service when setting entries) in seconds
app:
  cache:
    default-ttl-seconds: 300
server:
  port: 8080

spring:
  application:
    name: collaborative-pricelist-service
  neo4j:
    uri: bolt://neo4j:7687
    authentication:
      username: neo4j
      password: salescrm123

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true